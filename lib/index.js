/**
 * cau-portal 服务端 cordis 插件（阶段4第1步占位）：
 * 第2步在此注入 webServer + llm，开 /api/cau/health 与 /api/cau/enrich。
 */
export const name = 'cau-portal';
export const inject = [];
export function apply(ctx) {
    ctx.logger.info('[cau-portal] server plugin loaded (phase 4 step 1 placeholder)');
}
