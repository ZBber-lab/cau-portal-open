# 农大门户（cau-portal）项目复盘报告

> 日期：2026-08-31 下午 · 范围：全项目（服务端 / 客户端 / 数据管道 / MCP / 邮件 / 门户工具 / 仓库卫生 / 安装链）
> 方法：4 条只读代码审查轨道（服务端与构建链 A、客户端 B、数据管道 C、工具集 D）+ 运行时实测（MCP 工具、本机路由、GitHub API 远端树/运行记录）+ 使用场景闭环走查。
> 结论：**无 P0（无数据丢失 / 无链路全断 / 代码路径无密钥泄露）**；9 条 P1 + 约 30 条 P2。核心功能全部落实，但存在 1 个用户可见功能断点（未读计数）、1 个实测工具断点（MCP 两工具超时）、2 个闭环断点（邮件抑制）、1 处安装可复现性缺口（MCP 未打包）与远端仓库卫生问题（探索期 2000+ 文件误入仓）。

---

## 一、P1 发现清单（9 条，建议第一批修）

### 正确性 / 用户可见

1. **[闭环缺口] 「打开即已读」从未接线** —— `src/client/data.ts:284` 的 `markRead()` 全项目**零调用点**（grep 实证：仅定义处 1 处命中）；打开文章（panel-article 装载、panel-home:161 `openArt`、panel-column:113、toolview:91）都不写 readSet。结果：按钮未读计数（panel.tsx:81/249）与行内未读点（panel-home:180、panel-column:167）**永不随打开递减**，与 SPEC「打开即读、计数即时减一」直接冲突。
2. **[闭环缺口] 邮件日报的「测试发送/发送失败」会吞掉当日正式日报** —— `tools/email/service.mjs`：`sendReport` 无论 `mode='test'|'daily'` 都写 `last_sent`；`needDailyNow()` 只按「当天是否已发」判重。后果：① 用户 8:00 前点过「测试发送」→ 当天 8:00 正式日报被静默抑制；② 正式日报发送失败也写 `last_sent` → 当天不再重试（每分钟检查形同虚设）。建议：test 模式不写 `last_sent`；失败写 `last_attempt`、成功才写 `last_sent`。
3. **[实测断点] MCP `search_news` / `list_deadlines` 超时** —— 本会话实测两工具 `MCP error -32001: Request timed out`（`list_sites`/`list_latest`/`get_usage` 正常）。根因：`tools/mcp/index.mjs` 对全部条目**顺序逐个** `await readJson('articles/...')`（search_news:281-306 逐篇、list_deadlines:364-384 逐文件），GitHub 模式下一次 API 调用≈一篇，1351 篇文章 → 数百次串行请求必超时。数据量从 ~400 涨到 1397 后触发。建议：`list_deadlines` 改读 `summary.json` 的 deadline 清单；`search_news` 先按标题/摘要粗筛再按需取正文（或并行+缓存兜底）。

### 重合 / 冲突

4. **[重合冲突] 令牌双读路径** —— 未读计数/头部元信息/ensureToken 走旧键 `loadSettings().githubToken`（panel.tsx:76/86/96/244），数据读取走 `activeTokenValues()[0]`（data.ts:73/256）。用户从「设置→令牌管理」录入令牌后，按钮计数与面板头部**静默失效**。建议统一为 `activeTokenValues()[0]`。
5. **[口径不一致] 邮件🎯 与面板🏷 规则命中字段不同** —— `tools/email/report.mjs:66` 匹配 title+source+site_name+column_name/column，`src/client/data.ts:820` 只用 title+source+column。同一组规则，日报与面板命中结果对不上账。
6. **[零联动] `modules.v1.portal` 开关只落盘不改行为** —— 设置卡 `key:'portal'` 存在（settings.tsx:676-684），但全 client 无任何读取点（grep `modules.*portal` 0 命中）。要么接线（门户站点在面板隐藏），要么从设置页移除该卡。

### 安装可复现性 / 仓库卫生

7. **[打包缺口] `files` 缺 `tools/mcp`** —— package.json:13-20 只含 lib/tools/portal/tools/email/cordis.patch.yml/dsh.plugin.json/README.md。MCP 服务器（index.mjs + @modelcontextprotocol/sdk + zod）不进安装包；且 MCP 接线在 profile 补丁层用**机器绝对路径**（validate-config.mjs:30-32 硬编码 `D:\nodejs1\node.exe`、工作区路径）。当前仅本机可用，`dsh plugin add github:` 新装环境 MCP 半区缺失。
8. **[仓库卫生] 远端混入探索期产物与个人作业文件** —— GitHub API 实测远端 main 树：`explore/` **2008 个文件**（html 探针页、gh-push snapshots、dsh-std-main 参考源码、blob-raw.bin 等），根目录另有 `lishu_season_meteorology_daily_v2.csv`、`lishu_spring_tmax_2019_2024.png`、`plot_spring_tmax.py`、`test.py`（个人气象作业）。私有仓无外泄，但违背文件纪律（AGENTS §9）、**开源化前必须清除**。本地同款 4 作业文件 + `fix-log.txt` 仍存于工作区根（fix-log 尚未入远端，但下次 robocopy /E 会推入——推送前必须处理）。
9. **[卫生] `tools/tmp-*.mjs` 未被 gitignore** —— .gitignore 无 `tmp-*.mjs` 条目（robocopy /XF 有防护，但 `git add -A` 无防护；历史已 3 次 tmp 误入仓）。建议补 `.gitignore` 一行兜底。

---

## 二、P2 清单（按主题）

### 冗余（重复实现，一处改动易漏另一处）

| # | 重复项 | 位置 | 建议 |
|---|---|---|---|
| 1 | `tokenFromEnvOrYml` 逐字 ×3 | tools/portal/sync.mjs:21-35、tools/email/report.mjs:11-25、各类 tmp 探针 | 抽 `tools/lib/token.mjs` |
| 2 | HTTP 抓取+UA+重试两套 | tools/scraper/fetch.mjs:7-34 vs tools/portal/engine.mjs:68-86（重试次数/退避/超时全不同） | engine 复用 fetch.mjs，外包 CookieJar |
| 3 | 调度器骨架 ×2（timer/启动补抓/interval/unref/stop 从未接线） | tools/portal/sync.mjs:146-164 vs tools/email/service.mjs:154-165；src/index.ts 只调 start 不调 stop（无 teardown，HMR 泄漏 interval） | 抽 `makeScheduler()` + `ctx.effect` 清理 |
| 4 | GitHub Contents 读取层 ×4 | src/client/data.ts（直连）、src/index.ts `/api/cau/data`（代理）、tools/mcp/index.mjs（readSource/ghFetch）、tools/email/report.mjs（fetchJson） | 合并共享 readSource |
| 5 | `pathForm` ×2 | tools/scraper/crawl.mjs:62-70 vs prune.mjs:27-35 | 抽 util |
| 6 | 详情抓取+增量合并+feed 写盘块 ~40 行近同 | crawl.mjs:265-285（news）vs 362-383（boda） | 参数化抽公用函数 |
| 7 | `daysLeft` 两套实现（边界可差 1 天） | panel-home.tsx:35 vs panel-deadlines.tsx:9 | 合并 |
| 8 | `subscribeAttached` 与 `subscribeBus` 等价重复 | src/client/bus.ts:72/89 | 保留一个 |
| 9 | POST 样板（405/readBody/JSON.parse→400）逐字 ×5 handler | src/index.ts:143-154/176-187/242-253/349-360/375-386 | 抽 `readPostJson()` |
| 10 | build.mjs `inlineLocalRequires` 无缓存/去重（共享模块按引用次数重复转译+内联） | build.mjs:39-57（lib/client.js 已 797KB 膨胀、且是「单例须挂 window」根因） | 按 resolve 路径 Map 缓存 |

### 重合 / 冲突（其余）

- 未读口径=全部 `summary.important`——~~曾疑「无视 SPEC 近 7 天+高/中」~~ **已更正为误报**：crawl.mjs:201-204 生成 summary.important 时就已只收「高/中 + 近 7 天（时间无法解析则保留）」，客户端计数口径与服务端一致，无需修改。
- `mine` 与 `follow` 双重记录、移除不同步（data.ts:499/548）。
- context 开关未全联动：autoAttach 不判断 `mods.context`（panel-article.tsx:76），CtxBar 无条件注册。
- 两套 token 源并存：`/api/cau/data` 用客户端传入 token，服务端同步/日报用 profile token（一处过期后故障定位混乱，代理建议回退服务端 token）。
- 本地 `.github/workflows/crawl.yml` 副本仍含 30 分钟 schedule（与远端「workflow_dispatch only」漂移；推送靠 bat 的 `git checkout -- crawl.yml` 保底，风险留存）→ 建议把本地副本同步为远端版。
- `dsh.plugin.json` `entry.inject: []` 与 `src/index.ts` 的 `inject=['webServer','llm']` 不一致（清单过期）。
- View 类型联合缺 `'manage'/'deadlines'`（panel.tsx:44-50 vs 347/374）。
- 设置页令牌删除提示「Read」与实际需要 Contents 写矛盾（settings.tsx:851 vs data.ts:115）。
- rules→邮件同步失败静默 + 仅变更时同步（settings.tsx:573/592）。
- 「全部已读」不刷新 pill 计数（panel.tsx:242 vs panel-home:446）。
- 面板脚注「每 2 小时自动更新」无任何刷新实现（文案承诺与实现不符——要么实现自动刷新，要么改文案）。

### 不完善 / 边界

- **deadline 校验漏洞**：ai.mjs:111 `Date.parse('2026-02-31')` 非 NaN（滚动成 03-03）→ 不存在日历日被接受，待办卡日期错位。建议 `new Date(y,m-1,d)` 回读校验月/日。
- **空提交噪音**：crawl.yml 提交去重只排除 index.json；crawl.mjs/enrich.mjs 无条件重写 summary.json（last_updated 恒变）→ 每轮 Actions 都是一条「data: auto crawl」时间戳提交，「无变更」分支永不触发。enrich `done===0` 也重写（enrich.mjs:76-77）放大该问题。
- usage.jsonl 无轮转（appendFileSync），随 Actions 逐年累进膨胀；GH 模式下缺失时抛错而非返回零值（index.mjs:402 与工具描述矛盾）。
- smtp.mjs `rejectUnauthorized:false`（TLS 证书不校验，自用可接受、应注释说明）。
- engine.mjs session/account 非原子写（写一半崩溃会损文件）。
- `/api/cau/email/test` 恒 200（`out.ok ? 200 : 200` 死代码）；`/api/cau/portal/status` 失败也回 200——状态码契约不一致。
- sync 无变更返回 `{ok:false,error:'无变更'}` → 服务端日志每 6h 记一条「portal 同步失败：无变更」，掩盖真实故障。
- MCP GH_MODE 无本地回退；report.mjs:132 恒真排序比较器（死代码）；index.mjs:58 CATEGORIES 未使用；data.ts:408/481 `'pin'` 死分支。
- Actions push 无重试（sync.mjs 有 3 次 rebase 重试）→ 并发 push 低概率丢提交（下轮幂等自愈，但会误读「数据停了」）。
- crawl.mjs 无 `localOnly` 判定——与 SPEC「sites.json 加 portal 标 localOnly」的文档漂移（当前靠「portal 不入 sites.json」才自洽，是潜伏陷阱）。

### 遗留文件 / 死代码 / 合规

- `tools/scraper/task.bat`（8 行，硬编码 `D:\nodejs1\node.exe`，Windows 定时任务早已删除）→ 删除。
- `data/crawl-task.log`（写入方已消失）→ 删除。
- 门户三登录入口（面板 `/api/cau/portal/account`、login-cli.mjs、登录.cmd）→ CLI 两个可退役或归并。
- `tools/portal/inspect.mjs` 用相对路径写 inspect-report.txt（曾误入仓 11f3570）。
- `tools/mcp/smoke-test.ps1` 硬编码 `D:\nodejs1\node.exe`；`smoke-err.log` UTF-16 噪音；smoke-input 被 gitignore 但 ps1 依赖它 → 冒烟套装要么修要么删。
- `tools/sim-load.cjs` 已过时（只断言旧 inject/slot 子集，给「客户端完整可用」假信心）→ 扩充或标注退役。
- 死导出：`backfillFollowCaches`（data.ts:388）、`mineIdOf`（panel-deadlines:135）、`readArticle`（data.ts:571）。
- `tools/portal/des.js` = 2006 Guapo DESCore 第三方代码（无许可证头，`new Function` 载入）→ 开源前需加出处/许可说明。
- package.json `scripts.crawl/enrich` 指向未打包的 tools/scraper、无 `engines` 字段（ESM+全局 fetch 应声明 Node≥18）。

### 本地工作区（知晓即可，不阻塞）

- 本地 `data/` 陈旧（8/26，1141 条）vs 云端（8/31，1397 条）——设计如此（远端权威），但本地模式 MCP/爬虫冒烟会读到旧数据。
- `explore/`（443 文件）、`.push-clone/`、`node_modules`（43MB 开发工具）、`assets/brand-raw`（632MB）均在工作区，保留与否按既定计划。

---

## 三、使用场景闭环走查

| # | 场景 | 走查结论 |
|---|---|---|
| S1 | 每天开面板看要闻/待办 | 数据新鲜 ✅（云端 04:04Z）；**断点**：未读计数不减（P1-1）、未读口径偏宽（P2）、脚注「每2小时自动更新」无实现（P2） |
| S2 | 学校发新通知 → 进面板 | ✅ runs 全 success（12:00 桥 dispatch + 12:03 最后一轮原生 schedule，PUT 后 12:32/13:00/13:32 北京均无原生 schedule）；**噪音**：每轮空提交（P2） |
| S3 | 对话里问「最近有什么讲座/截止」 | **断点**：search_news/list_deadlines 实测超时（P1-3）；list_sites/list_latest/get_usage ✅ |
| S4 | 关注关键词命中 → 🎯 + 系统通知 | 面板侧 ✅；**冲突**：邮件🎯与面板🏷口径不一致（P1-5） |
| S5 | 门户登录过期 | ✅ 会话自愈+自动重登已实锤（SPEC 8/30 生产验证）；小风险：凭据非原子写（P2） |
| S6 | 每天 8:00 收邮件日报 | **断点**：测试发送/失败会抑制当日日报（P1-2） |
| S7 | 数据管理删除旧数据 | ✅ 闭环完整（本地隐藏→prune 请求→Actions 真删→清单清空）；小矛盾：权限文案（P2） |
| S8 | 文章引用到对话（双向协同） | ✅ 分段闭环（引用→药丸→注入→工具结果卡）；context 开关联动不全（P2） |
| S9 | 换电脑/重装插件 | 面板+服务端 ✅；**断点**：MCP 半区未打包+绝对路径（P1-4） |
| S10 | 长期关机后开机 | 门户补抓 ✅、邮件补发 ✅（但补发受 P1-2 抑制影响） |

---

## 四、核对过的无问题项（避免误判）

- 服务端 11 条路由 ↔ 客户端调用一一对应；`lib/index.js`/`lib/client.js` 与 src 同步（类名/路由逐条 grep 命中）；`/api/cau/summary` 404 属正常（客户端不调用该路由，数据走直连/代理）。
- 日志不泄露密钥；statusInfo 只回 hasCode 布尔；.gitignore 对门户/邮件凭据、*.log 覆盖完整（git check-ignore 实核命中）。
- index/summary 仅 Actions 写；portal sync 只提交 portal feed/文章，无互踩；Actions concurrency 同组串行。
- usage.jsonl 仅 ai.mjs logUsage 写（MCP 只读），无重复计费。
- 三 parser（news/boda列表/boda详情）按 CMS 拆分无重叠；price.json 是 ai.mjs 价表（非死文件）；prune 实现仅一处。
- 调度切换：PUT 后无原生 schedule ✓（终验 14:00 桥 dispatch 于本报告写作时尚未到点，稍后补记）。
- 面板 portal L2 专属卡、归档/剪除过滤、数据管理删除、引用协同的本地闭环均核对通过。

---

## 五、修复批次建议（供决策，按攒批规则执行）

- **批 1「正确性 + 卫生」（小改、一次推送）**：P1-1（markRead 接线）、P1-2（邮件 last_sent/last_attempt）、P1-3（MCP deadline 走 summary、search 粗筛）、P1-4（files+tools/mcp、路径相对化）、P1-5（规则口径统一）、P1-6（令牌双读统一）、P1-7（portal 开关接线或撤卡）、远端卫生（`git rm -r explore` + 4 作业文件 + .gitignore 补 tmp-*.mjs）+ 本地同款清理。
- **批 2「重构去重」（可选，工作量中等）**：P2 冗余表 1-10 + deadline 日历校验 + summary 空提交 + usage 轮转。
- **批 3「遗留清理 + 合规」（可选）**：task.bat/inspect/login-cli/登录.cmd/smoke 套装/sim-load 更新或退役、des.js 许可注释、scripts/engines、dsh.plugin.json inject 同步、本地 crawl.yml 副本同步。
- **不修**：本地 data 陈旧（设计使然）、brand-raw 保留（用户已定）、按钮位置（留待 UI 优化阶段）。

---
## 修复进展（本地已改、待批次推送；2026-08-31 下午）

- **批 1 ✅（用户验收通过）**：P1-1 markRead 接线（openArticle/replaceArticle + recountUnread）· 全部已读→计数同步清零（HomeView onReadChange 回调）· P1-5 令牌双读统一（activeTokenValues()[0]，ensureToken 写 tokens.v1）· P1-4 files 加 tools/mcp + README 说明。构建通过、bundle grep 验证。
- **批 2 ✅（用户验收通过）**：P1-2 邮件抑制修复（last_test/last_attempt 与 last_sent 分离、10 分钟失败退避、test 不占位）+ 今天下午补发一封正式日报当验收信号；P1-3 MCP 超时修复（list_deadlines 改读 summary.deadlines；search_news 两阶段=标题+AI摘要粗筛+限 40 篇并发 6 正文回退；删死函数 listArticleFiles）——冒烟实测：9 调用 8.2s 全通（此前两工具 30s+ 超时）、search_news 单独 30 命中秒回、list_deadlines 30 天窗口 5 条；P1-6 规则口径统一（面板 matchRules 与邮件 matchRule 均取 标题+来源+站点名+栏目名+栏目key、全不区分大小写）——report.mjs CLI 实测「推免」规则 🎯 段 2 命中。
- **批 3 ✅（本地完成待验收）**：P1-7 portal 开关接线（用户更正理由：要闻已分两支、不存在占满问题；接线理由=假按钮必须落实）——关掉后要闻隐藏校内平台支、栏目频道隐藏统一门户入口、今日要览/未读计数排除门户、直进门户栏目给提示（MCP 不受影响）；P1-8 远端卫生本地部分完成（explore/ 443 文件已删、FEASIBILITY.md 移至 docs/、.gitignore 补 explore/ 与 4 作业文件；**远端 git rm 随下次推送执行**）；P1-9 四项全做（.gitignore 补 tools/tmp-*.mjs、dsh.plugin.json inject 同步 webServer+llm、面板脚注改「打开时拉取」、本地 crawl.yml 副本同步为 workflow_dispatch-only）。
- **已更正误报**：P2「未读口径偏宽」撤销（summary.important 服务端已按高/中+近7天过滤）。
- 全部修复均本地完成、构建与冒烟验证通过，**待一次性推送**。
